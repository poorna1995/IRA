# ── Core ──────────────────────────────────────────────────────────────────────
datasets>=2.20.0,<3.0.0          # HuggingFace datasets
huggingface-hub>=0.24.0          # token auth + caching
pyarrow>=14.0.0                  # columnar storage backend
pandas>=2.1.0                    # DataFrame ops in preprocessing

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.6.0                  # schema validation & typed models

# ── Config management ─────────────────────────────────────────────────────────
omegaconf>=2.3.0                 # structured YAML configs (hierarchical)
hydra-core>=1.3.2                # config composition + CLI overrides

# ── Storage ───────────────────────────────────────────────────────────────────
# pyarrow already pulled above; used for Parquet I/O

# ── Logging / observability ───────────────────────────────────────────────────
loguru>=0.7.2                    # structured logging with rotation
rich>=13.7.0                     # pretty CLI output + progress bars
tqdm>=4.66.0                     # progress bars in loops

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0

# ── Type stubs ────────────────────────────────────────────────────────────────
types-tqdm